export type DefaultColorsModel = {
    upcoming: {
      borderColor: string;
      borderWidth: number;
      textColor: string;
      textSize: number;
      backgroundColor: string;
      borderRadius: number;
    };
    current:{
        borderColor: string;
      borderWidth: number;
      textColor: string;
      textSize: number;
      backgroundColor: string;
      borderRadius: number;
    };
    inactive:{
        borderColor: string;
      borderWidth: number;
      textColor: string;
      textSize: number;
      backgroundColor: string;
      borderRadius: number;
    },
    // Add more categories if needed
  };